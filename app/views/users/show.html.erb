<h1>ここはユーザー詳細ページ</h1>
<main>
  <p id="notice">Welcome! You have signed up successfully.</p>
  <div class="row">
    <div class="col-md-3">
<h2>User info</h2>
 <% user.each do |uesr| %>
<div>
  
  <p>投稿ユーザー画像：<%= image_tag('sample-author1.jpg') %></p>
  <p>ユーザーネーム：<%= users.user.name %></p>
</div>
<% end %>

<h1>New book</h1>

<%= form_with model: @book, url:'/books', local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
            